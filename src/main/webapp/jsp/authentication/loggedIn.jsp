<%@include file="/jsp/common/include.jsp"%>
{
	"id" : "${publisher.id}",
    "sessionExpiry" : "123",
    "token" : "${publisher.token}",
    "name" : "${publisher.name}",
    "email" : "${publisher.email}"
}